node_modules/
.venv/
/dist
.DS_Store
*.log

# Local deploy secrets — NEVER commit. Copy .env.deploy.example → .env.deploy
.env.deploy
.env*.local
.dev.vars*

# Pulled review comments — local working copy, never committed.
review-comments.local.md

# Wrangler local state
.wrangler/
