# GoVocal Components — manifest (recall index)

> **Internal / never ships.** This file is the *lightweight index* of the composed
> component library. When building a prototype, scan this table, then open the one
> component file you need (don't load them all). Snippets live in
> `skills/govocal-ui/components.md`; the `.gv-*` styling lives in canonical
> `govocal-primitives.css` (atoms) + `govocal-ui.css` (FO) / `govocal-bo.css` (BO).
> Live demos are shipped under `/components/<name>/` (Components tab).

Each component is a demo folder under `components/<name>/` that **references the
canonical assets directly** (`../../skills/govocal-ui/<asset>`) — it never copies or
forks them, so it always reflects the live source. Canonical CSS = `govocal-tokens.css`
→ `govocal-primitives.css` (shared atoms) → `govocal-ui.css` (FO) / `govocal-bo.css` (BO).
Edit those; `npm run lint` enforces that no demo copies or redefines them. (Only
*prototypes* copy assets — they're the tier allowed to fork.)

| Component | Folder | Key classes | What it is |
|---|---|---|---|
| Header + nav | `components/header-nav/` | `.gv-header`(`__inner` full-width) `.gv-nav`/`.gv-nav__list` `.gv-nav__dd`/`.gv-nav__menu`(`.gv-menu-sep`) `.gv-nav-m` · signed-in: `.gv-account-dd`/`.gv-account`/`.gv-account__name` `.gv-avatar` `.gv-iconbtn__badge` `.gv-lang` · switch: `.gv-auth-out`/`.gv-auth-in` + `data-auth` | Responsive **full-width** 78px site chrome (mirrors live `#e2e-navbar`): logo flush-left, `<nav aria-label="Primäre">` primary nav, dropdown + “Mehr ···” overflow, actions flush-right; CSS-only hamburger drawer (real filled-bar icon) < 860px. **One component, two states** flipped by `data-auth` on a root: signed-out (search + Sign-in CTA) and signed-in (search · notification w/ count badge · **account dropdown** [avatar+name → Manage platform · My activity · My settings · Sign out] · **language switcher**). **Active item** = top accent bar + subtle tenant-tinted cell, **weight 500 (not bold)**, matching the live product; hover / open-dropdown get the grey cell highlight. |
| Footer | `components/footer/` | `.gv-footer` `.gv-footer__links` `.gv-powered-logo` | Source-grounded on `#hook-footer`: secondary-nav `<ul>` of legal links (Cookie-Einstellungen is a `<button>`) + “Ermöglicht durch go·vocal” attribution. No tenant logo. Links left / attribution right, stacks < 720px. |
| **Footer logo band + dual-auth header + brand-tinted nav** (config variants) | `components/footer-logos/` | `.gv-footer__logos`(`.row`/`__logos-cap`) · `.gv-nav.tinted` · `.gv-auth-dual` (composes `.gv-btn.primary` + `.gv-btn.primary-inverse`) · reuses `.gv-footer`/`.gv-header`/`.gv-nav`/`.gv-lang` | **Three optional header/footer CONFIG variants**, source-grounded on Luxembourg (`zesumme-vereinfachen.lu`, capture `fo-lux-footer-logos`), cross-checked vs Linz (`#hook-footer-logo` single tenant logo). Verified via `fo-lux-footer-logos/{logo-band,header-chrome}`. **(1) `.gv-footer__logos`** = an OPTIONAL white centered **logo band** rendered as a SIBLING above `<footer>` (measured `padding 50px 20px 20px`, logos `height:100px`); default **column** (single tenant logo), **`.row`** for tenant + partner/government marks side-by-side, **`.gv-footer__logos-cap`** shrinks partner logos; tenant logo is a `data-gv-logo` slot. **(2) `.gv-nav.tinted`** = primary-nav labels in `var(--gv-tenant-primary)` (the only delta vs base `.gv-nav` — weight 500/16px + active/hover states unchanged). **(3) `.gv-auth-dual`** = the signed-out header's DUAL auth CTA — composes the pre-existing filled `.gv-btn.primary` log-in + inverse `.gv-btn.primary-inverse` register (label re-tinted to the brand), beside the `.gv-lang` switcher. **Powered-by (`.gv-footer__powered`) is OPTIONAL** — keep for tenants that show "Généré par Go Vocal" (Lux does), omit where suppressed. **Cross-tenant SYSTEM** = the band/nav/CTA geometry (token-aliased); SKIN = logos / brand hue / language, re-themed per `?theme=`. Net-new = the `.gv-footer__logos` band + `.gv-nav.tinted` + `.gv-auth-dual` (`govocal-ui.css`) — base footer/nav/buttons untouched. Pure assembly. |
| Project card + rail | `components/project-card/` | `.gv-rail` `.gv-pcard` (`.wide` `.square`) · **boxed:** `.gv-pcard.boxed` (`.horizontal`) `.gv-pcard__body`/`__spacer`/`__foot`/`__progress`/`__count` · grid `.gv-pgrid`(`.span-2`/`.span-3`) | Participation-project card (thumb, title, status meta, CTA). Two layouts: the **rail** (`.gv-rail` + borderless `.gv-pcard`) and the **boxed grid** card (`.gv-pcard.boxed` in a `.gv-pgrid` — bordered/elevated, padded `__body`, equal-height, with `.horizontal` full-width feature variant + folder `__count` badge). Stretched-link card, no nested anchors; boxed CTA is a real `.gv-btn` above the link. |
| **Folder card** | `components/folder-card/` | `.gv-pcard.boxed.folder` · `.gv-pcard__fmedia`/`__fbody`/`__fdesc`/`__fpile`/`__fthumb` · re-grounded `.gv-pcard__count` (folder glyph + green number + "N projects", pinned top-right) · token `--gv-folder-count` | **The projects-list folder card**, re-grounded on the live St Louis card (`.e2e-folder-card`, `stlouis.govocal.com/projects`), verified via `fo-stlouis-folder/{card,count,description}`. White **borderless** 3px card on the soft `--gv-shadow`, padding `18px 0 25px`, **min-height 540px** (grows ~583px stacking child-project previews): a project **hero image** with the project-**count badge** (folder glyph + green number + `N projects`) pinned **top-right** over a dark overlay, then the folder **title**, a 16px/300 cool-grey **description preview**, then small child-project avatar previews. SYSTEM = the geometry (all `--gv-radius`/`--gv-shadow`/spacing tokens); SKIN = the count colour (`--gv-folder-count`, St Louis `#358545`) + font/palette/imagery, re-themed via `?theme=`. Net-new = the `.folder` variant + `__fmedia`/`__fbody`/`__fdesc`/`__fpile`/`__fthumb` + the re-grounded `.gv-pcard__count` (`govocal-ui.css`) + the `--gv-folder-count` token — base `.gv-pcard.boxed` untouched. Pure assembly. |
| **Project carousel** | `components/spotlight-carousel/` | `.gv-carousel`/`__head`/`__title`/`__controls`/`__scroll`(`.disabled`/`[aria-disabled]`)/`__skip` · **wraps** `.gv-rail`(`.rail--fade-left`/`-right`) + `.gv-pcard.light` + `.gv-title.h2` + `.gv-sr-only` | **The product-default homepage "project row"** — a section title + prev/next scroll controls over a **paged** horizontal rail of cards. Source-grounded on Falkirk (also St Louis); **structurally common across tenants** (one product, re-skinned), so `.gv-carousel` is a **thin wrapper around the canonical `.gv-rail` atom** — the rail keeps every scroll/snap/fade mechanic; the carousel only adds the header (`.gv-title.h2` + transparent `.gv-carousel__scroll` 24px-chevron prev/next, **left starting `.disabled`**) and the live a11y scaffolding: `role="region"` + `aria-roledescription="carousel"` + the sr-only **"Press escape to skip carousel"** `.gv-carousel__skip` (extends `.gv-sr-only`, shown on focus). Scroll control matches the live `.e2e-event-previews-scroll-*` wrapper (bare transparent — no own bg/border/radius/shadow — centered flex), verified via `fo-falkirk-spotlight-carousel/scroll-control`. Cards (`.gv-pcard.light` here, any `.gv-pcard` works) drop in unchanged. **Net-new = just the `.gv-carousel` wrapper in `govocal-ui.css`** — `.gv-rail`/`.gv-pcard`/`.gv-sr-only` untouched. Pure assembly; re-skins per `?theme=`. |
| **Homepage widgets** | `pages/homepage/` | `.gv-hero__cta` · `.gv-spotlight`/`__inner`/`__eyebrow`/`__title`/`__lead`/`__actions`/`__media`(`--placeholder`)/`__chip` · `.gv-bubbles`/`.av`/`.count`/`__label` · `.gv-progress`/`__fill` · `.gv-ptoolbar` `.gv-tabs`/`.gv-tab` `.gv-filterbar`/`.gv-filter-btn` · `.gv-showmore` · **events:** `.gv-events__head`/`__grid`/`__empty` · `.gv-event-card`/`__media`/`__date`(`.m`/`.d`/`.y`)/`__rsvp`/`__body`/`__title`/`__row` · `.gv-ctaband`/`__inner`/`__title`/`__lead` · `.gv-prose` · `.gv-status-label.finished` · **image-less card:** `.gv-pcard__thumb.icon` (real grey + institution glyph) · **branded tiles:** `.gv-pcard__thumb--placeholder`/`--forest`/`--ocean`/`--clay`/`--sage`/`--civic`/`--coral`/`--sand` · `.gv-thumb-cap`(`--lg`/`--sm`/`--xs`) · `.gv-pcard__badge` · **Wien light cards:** `.gv-pcard.light` (borderless: rounded thumb + 18px/700 title + count/desc) · `.gv-pcard__status`(`.done`) (clock=tenant + #333 "no end date" / green-check + green "N weeks ago") · `.gv-pcard__more` ("Learn more" link) | The modern GoVocal landing-page anatomy, source-grounded on the live signed-in homepage (uxusertesting.govocal.com, Raleigh) + the **events widget** (community.govocal.com) + the **Stadt Wien** light-card rows (mitgestalten.wien.gv.at): banner overlay **CTA** pill, a "currently working on" **spotlight** (copy + media), **avatar bubbles + count**, **time-remaining progress bar**, the **projects toolbar** (status tabs + Tag/Area filter selectors), **show-more**, a populated **events grid** of real **event cards** (two-tone date chip · clock/link/registrants · Register) + empty-state variant, **branded-emblem "climate teams"** row + **"completed projects"** row (both `.gv-pcard.light` — borderless, status line + Learn more), **proposals CTA band**, and a **rich-text** block. **Faithful + pure assembly** — image-less cards use the real `.gv-pcard__thumb.icon` grey-institution placeholder (not invented gradients); branded tiles are the Wien-style artwork. A reference homepage carrying **all** module types across tenants. Verified via `homepage-*` checkpoints. Demoed end-to-end on the **Homepage** page. |
| **CTA banner (full-width strip)** | `components/cta-banner/` | `.gv-cta-banner`/`__inner`/`__cta` (fill `--gv-cta-banner-bg` default `--gv-tenant-primary`) · reuses `.gv-btn.primary-inverse` + `.gv-cb-frame`/`.gv-cb-row.cols-1` | **The Content-Builder "CTA banner" block** — a thin, full-bleed coloured strip carrying ONE centered call-to-action button, source-grounded on St Louis (`stlouis.govocal.com`, capture `fo-stlouis-cta-banner`, tenant-primary `#033D8B`), verified via `fo-stlouis-cta-banner/button-wrap`. The band fill follows `--gv-tenant-primary` (override `--gv-cta-banner-bg` for a one-off); `__inner` is the centered flex row capped at the CB content measure with thin `22px 24px` strip padding + the measured 14px/19.999/`rgba(0,0,0,.87)` baseline; `__cta` is the transparent centered wrapper around a real base `.gv-btn` `<a>` (9px 18px / radius 3px / 45px tall), `.primary-inverse` so it reads on the fill. **Distinct from `.gv-ctaband`** (transparent centered title+lead+CTA) and **`.gv-monitorband`** (tinted 3-zone text/media/CTA card) — this is the bare coloured strip whose sole content is the button. Mounts as a `.gv-cb-row.cols-1` cell in a `.gv-cb-frame`. **Net-new = just the `.gv-cta-banner` block (`govocal-ui.css`)** — no base mutated. Pure assembly; re-skins per `?theme=`. |
| **Featured 3-up spotlight row** | `components/homepage-featured-row/` | `.gv-featured-row` · `.gv-pcard.featured` (new variant) · reuses `.gv-pcard__thumb`/`__title` + `.gv-bubbles.xs` + `.gv-title.h2` | The homepage **"We want to hear from you"** row — three large, **image-led** featured project cards that sit ABOVE the regular project grid, under a shared `.gv-title.h2` (25px/700, margin-bottom 16). Each card is **borderless + photo-dominant**: a **4:3** thumb (live 376×282, radius 3, clipped) + an **18px/700** title 8px below + an avatar `.gv-bubbles.xs` row — **NO** progress/timer/status (that's `.gv-pcard.boxed`), and **distinct** from the single wide-block `.gv-spotlight`. Source-grounded on wietsedemo `/en` (capture `fo-homepage-featured-row`), verified via `fo-homepage-featured-row/{section-title,card-title}`. Net-new = the `.gv-featured-row` grid + the `.gv-pcard.featured` variant (`govocal-ui.css`) — base `.gv-pcard`/`__thumb`/`__title` untouched. Pure assembly; collapses to 1 column < 860px; re-skins per `?theme=`. |
| **File attachment (download row)** | `components/attachment/` | `.gv-attachment`/`__icon`/`__name`/`__size` · `.gv-attachments` (list) · icon `paperclip` · `.gv-prose h2`/`h3`/`strong` + `.gv-cta-inline` · token `--gv-attachment-border` | **The Content-Builder "file attachment" download row** on a custom / info page (terms / privacy / about / archive). Source-grounded on St Louis (`stlouis.govocal.com/pages/memos`, capture `fo-stlouis-infopage`), verified via `fo-stlouis-infopage/attachment-{row,name,size}`. An outlined row (1px `#C8D0D7` = `--gv-attachment-border`, 3px radius, pad `10px 20px` — digest #11) holding a 24px **paperclip** glyph (net-new icon in `govocal-icons.js`), the file-name `<a download>` link + a `(NNN KB)` size span (both 16/400/24/`--gv-cool-grey-600` — digest #12/#13). Stack in `.gv-attachments`. Net-new also = the **info/custom-page rich-text** extras on the shared `.gv-prose` (embedded `h2` page sub-heading 25/700/32.5/#333 — digest #4 — + `h3`/`strong`) and the inline `.gv-cta-inline` button (tenant-primary fill / white / 3px / `10px 18px` — the live `a.custom-button`, digest #10). **Cross-tenant SYSTEM** — every tenant routes `/pages/<slug>` through this one layout (global chrome + left-aligned constrained `.gv-prose` column on the `--gv-bo-canvas` page bg, **NO hero**); the SKIN re-themes per `?theme=`. Net-new in `govocal-ui.css` + `--gv-attachment-border` token + `paperclip` icon — no base mutated. Full page assembly: `pages/fo-stlouis-infopage/`. |
| Hero / banner | `components/hero/` | `.gv-hero`(`.signed-out`/`.centered`/`.layout-tworow`/`.layout-fixed`) `.gv-hero__media` `.gv-avatars` | Full-bleed page banner: tenant-tinted overlay, title/lead, avatar+count stack, CTA. Image-agnostic. **Homepage-banner layouts:** the admin Homepage-banner widget's three layouts — full-width (base `.signed-out`), `.layout-tworow` (photo row + white text band, filled CTA; needs `.gv-hero__media`), `.layout-fixed` (shorter fixed-ratio overlaid strip) — × signed-out (avatars + sign-up) / signed-in (no avatars, own CTA). Live toggle across all states in `pages/homepage/`. |
| **Signed-out hero (full-width banner)** | `components/signed-out-hero/` | `.gv-hero.signed-out`(`.centered`)/`__inner`/`__title`/`__lead`/`__actions` · `.gv-avatars`/`.av`/`.count` · `.gv-btn.primary-inverse` | **The home-page banner a VISITOR sees before signing in** — a cross-tenant STRUCTURAL pattern, source-grounded on St Louis (`stlouis.govocal.com`, capture `fo-stlouis-home`), verified via `fo-stlouis-home/{banner,content-col,subtitle,cta-inverse}`. A fixed **450px** full-bleed photo + tenant-primary overlay @ .6 (re-themes via the base `.gv-hero::before`) with a centered (`.centered`) content column (`padding 50px 30px`, max-width 1012): white H1 30/700 + subtitle 18/400 + an avatar cluster with a **numeric overflow** `.count` bubble + a single `.gv-btn.primary-inverse` CTA. Fallback canvas **`#EDEFF0`** (`--gv-bo-canvas`) shows with no `--gv-hero-image`. **Net-new = the `.gv-hero.signed-out`/`.centered` variant (`govocal-ui.css`) + `.gv-btn.primary-inverse` geometry promoted into `govocal-primitives.css`** (white #fff / `rgba(0,0,0,.87)` / 1px transparent / radius 3 / 14px / pad 13 22) — base `.gv-hero`/`.gv-btn` untouched. Pure assembly; re-skins per `?theme=`. |
| Modal + login | `components/login-modal/` | `.gv-modal-overlay`/`.gv-modal` `.gv-modal__header`/`__title`/`__close`/`__body` `.gv-or` | Reusable dialog abstraction (overlay → card → title header + close + scrollable body), shown via GoVocal’s real “Before you participate” auth flow (email + “Or” + Google). ARIA on the card; primitives in body. |
| **Cookie-consent modal** | `components/cookie-modal/` | `.gv-cookie__content`/`__icon`/`__title`/`__body`/`__link`/`__manage` · `.gv-modal__footer` (reusable 3-action row) · reuses `.gv-modal-overlay`/`.gv-modal` + `.gv-btn.text`/`.gv-btn.primary` + icon `cookie` | **The global cookie blocker as a CONTENT variant on the `.gv-modal` shell** (no new shell), source-grounded on Stadt Wien (`mitgestalten.wien.gv.at`, capture `fo-wien-cookie-modal`), verified via `fo-wien-cookie-modal/{card,title}`. Reuses `.gv-modal-overlay`/`.gv-modal` (scrim `rgba(0,0,0,.75)`, white **650px** card, **3px** radius, top-aligned). New = the content layout: `.gv-cookie__content` (30px pad) holding a tenant-tinted product **cookie icon** (`data-gv-icon="cookie"`, real `fill="#ff5a64"` → `--gv-tenant-primary`, net-new icon in `govocal-icons.js`) + `__title` (21/700/27.3/#333) + `__body` (16/400/24/#333) with a tenant-primary `__link`; plus a **3-action footer** `.gv-modal__footer` (pad `16px 28px`, reusable on any modal) — **Manage** (`.gv-btn.text` ghost, pushed left via `__manage`) / **Reject** / **Accept** (`.gv-btn.primary`, tenant-filled). No top-right close (a consent prompt blocks until answered). Net-new in `govocal-ui.css` + the `cookie` icon + a base `.gv-modal` 14px/19.999 text default in `govocal-primitives.css` (matches the real `.modalcontent`; no checkpoint regressed). **Cross-tenant SYSTEM** — every tenant routes its consent prompt through this dialog; the SKIN (icon tint/font/language) re-themes per `?theme=`. Distinct from `govocal-cookies.js` (the bottom-bar mechanic). Pure assembly. |
| Community Monitor | `components/community-monitor/` | `.gv-modal.size-monitor` `.gv-modal__close.round` `.gv-monitor__intro`/`__question`/`__count` `.gv-sentiment-scale`/`__opt`/`__face`/`__cap` | The “City at a glance” satisfaction module — ongoing-survey modal/banner asking how residents feel about governance & public services as a **5-point emoji sentiment** scale. Grounded on wietsedemo (`.modalcontent` 460px card · `#community-monitor-modal-title` cool-grey intro · `#<q>-linear-scale-option-N` 56px tenant-primary options · round 46px close). Reuses the `.gv-modal` shell; `.gv-sentiment-scale` is a NEW horizontal emoji variant (sibling of the survey kit’s vertical `.sv-sentiment`, not a mutation). |
| **Embedded-survey band** | `components/homepage-survey-band/` | `.gv-monitorband`/`__inner`/`__text`/`__title`/`__lead`/`__media`/`__cta` · reuses `.gv-btn.primary` | The homepage **"Help us serve you better"** banner promoting the ongoing community-monitor survey — the **SISTER surface of the Community Monitor modal** (in-page band vs. pop-up). A 3-zone tinted flex card: text column (h2 25/700/#333 + lede 16/400/#333) · 240px sentiment-question **preview image** · tenant-primary **Take the survey** CTA. Source-grounded on wietsedemo `/en` (capture `fo-homepage-survey-band`): `__inner` tint = tenant-primary @ 10% (`--gv-tenant-primary-lighten90`), `padding 32px`, `gap 16px`, `radius 16px`; CTA = base `.gv-btn.primary` (9px 18px / radius 3px). **Distinct from `.gv-ctaband`** (centered single-column proposals band) — a NEW `.gv-monitorband` variant in `govocal-ui.css`, no base mutated. Re-skins per `?theme=`. Verified via `fo-homepage-survey-band/{card,title,cta}`. |
| **Monitor band — duration meta + stacked-chip confirm (Linz)** | `components/fo-linz-monitorband/` | `.gv-monitorband__ctameta--duration` (additive modifier) · reuses `.gv-monitorband`/`__inner`/`__text`/`__cta` + `.gv-event-card.bordered`/`.gv-event-datechip--stacked` | **Cross-tenant CONFIRM + small additive variant**, source-grounded on Linz (`partizipation.linz.at`, capture `fo-linz-monitorband`, tenant-primary `#604596`). The community-monitor band uses the IDENTICAL formula to wietsedemo (`div.lhezhc` = `rgba(96,69,150,.1)` / pad 32 / gap 16 / radius 16) — net-new is ONLY the optional **`.gv-monitorband__ctameta--duration`** modifier: the "Dauer 2 Minuten" duration line (`p.sc-ktEKTO` = 14px/**400**/14px/tenant-primary), same clock affordance as the base `__ctameta` but the measured weight 400 / tight line-height (base "Takes N minutes" is weight 600). Additive, base untouched (`govocal-ui.css`). The capture ALSO confirms the bordered EventsWidget **stacked date-chip** year band (`div.ilUTbV` = `#604596` fill / white / radius `0 0 3px 3px` / pad 6px) feeding B4 — `.gv-event-datechip--stacked` needed no change. Verified via `fo-linz-monitorband/band` (geometry; the lone non-geometry diff is the srgb/rgba serialization equivalence of the shared `--gv-tenant-primary-lighten90` token). Pure assembly; re-skins per `?theme=`. |
| Survey fields | `components/survey-fields/` | `.sv-optcard` `.sv-rating` `.sv-scale` `.sv-sentiment` `.sv-rank` `.sv-imggrid`/`.sv-imgcard` `.sv-matrix` `.sv-map` `.sv-drop` · engine: `GVSurvey.field()` / `GVSurvey.mount()` | Every GoVocal input-form / survey **question type** (text · multiline · number · select · multiselect · rating · ranking · linear_scale · sentiment · image-select · matrix · map point/line/polygon · file/shapefile upload) **plus** the page-by-page runner (wizard, progress bar, Next-gating). Shared source = `skills/govocal-ui/govocal-survey.css` + `govocal-survey.js` — a self-contained kit on top of the gv-* primitives (its **own** stylesheet + JS, not folded into govocal-ui.css). Recall ONE widget: `el.innerHTML = GVSurvey.field({type:'matrix', …})`; build a whole survey: `GVSurvey.mount(el, FORM)`. Themeable `--gv-*`. Powers the **Input Form** page. |
| **Back-office sidebar** | `components/bo-sidebar/` | `.gv-bo-side`(`.is-rail`)/`__brand`/`__logo`/`__brandtext` · `.gv-bo-nav`(`--bottom`)/`__item`(`.is-active`)/`__icon`/`__label`/`__sub` · `.gv-bo-avatar` `.gv-bo-chev` `.gv-bo-count` · shell: `.gv-bo-shell.is-rail` | **The BO sidebar as a standalone, responsive component.** Measured live: **224px extended** (teal `#147985` brand band over navy `#003349`; 24px link-blue icons + white labels; active = dark cell) and an **80px icon rail** (`.is-rail`) it collapses to at **≤1200px** (logo-only, centered icons, active = inset rounded cell, count badge overlaps the bell). Default extended; add `.is-rail` to force the rail, or mark `data-gv-side-auto` + the matchMedia snippet to auto-collapse. Embedded by **bo-app-shell**. |
| **Back-office app shell** | `components/bo-app-shell/` | scope `.gv-bo` · `.gv-bo-shell` `.gv-bo-side`/`__brand`/`__logo` `.gv-bo-nav`(`--bottom`)/`__item`(`.is-active`)/`__icon` `.gv-bo-count` · `.gv-bo-topbar`/`__title`/`__actions` `.gv-bo-meta`/`__item` · `.gv-bo-tabs`(`--sub`)/`.gv-bo-tab`(`.is-active`)/`__new` | **The first BACK-OFFICE component.** The staff-facing chrome shared by every BO screen: dark teal/navy sidebar (icon+label nav, active cell, red count badge), project top-bar (title · status pills · actions), and the project tab row. GoVocal's own fixed theme — **not** city-themed. The `.gv-bo` scope remaps `--gv-tenant-*` → the fixed BO palette (`govocal-bo.css` + `--gv-bo-*` tokens), so existing primitives (`.gv-btn.admin-dark`, `.gv-iconbtn`, inputs, focus) render in BO colours unchanged. Phase ribbon, sub-tabs, tables and stat cards mount inside `.gv-bo-main`. |
| Phase timeline | `components/phase-timeline/` | `.gv-phases__bar` `.gv-pnav`/`.gv-dotmark` `.gv-stepper` `.gv-phase`/`.gv-pstep`/`.gv-pstep__dot`/`.gv-phase__label` `.gv-phasepanel`/`__num`/`__name`/`__date` | GoVocal’s project phase nav: a connected row of **chevron/arrow “ribbon” segments** (interlocking, number inside, label below). **Two ORTHOGONAL states** — never conflate them: the **current** phase = mint fill + dark-green number + **dot** (class `.current`, shown ALWAYS, even when not being viewed); the **viewing/selected** phase = dark-slate fill (`aria-selected=true`, an INDEPENDENT overlay that wins when its phase is being viewed). Three-way render: other = grey track, viewing = slate, current = mint+dot. Pick `selected != current` so both states show (a current phase you are also viewing renders slate). 3-button nav (Previous · Current · Next) top-right. Content panel leads with a big green numbered circle + green name + date. `role=tablist`/`tab`/`tabpanel`. |
| **FO project method bodies** (Pages) | `pages/project-page/` · `project-proposals/` · `project-survey/` · `project-volunteering/` · `project-common-ground/` | `.gv-idealist` · `.gv-ideacard`/`__thumb`/`__body`/`__title`/`__head`/`__avatar`/`__meta`/`__author`/`__sep`/`__time`/`__excerpt`/`__foot` · `.gv-react`/`__btn`/`__count`/`__comment` · `.gv-feed`/`__count`/`__col` `.gv-feedfilter`/`__group`/`__label`/`__btn`/`__count` · `.gv-viewseg`/`__tab` · `.gv-threshold`/`__icon`/`__count`/`__bar`/`__fill` · `.gv-statuspill`(`.expired`/`.open`) · `.gv-cause`/`__media`/`__img`/`__badge`/`__body`/`__title`/`__count`/`__desc`/`__cta` · `.gv-surveyband`/`__inner`/`__status`/`__dot`/`__cta` · `.gv-partbar`/`__inner`/`__status` · `.gv-phasebody--full` · `.gv-btn.on-color` `.gv-btn.volunteer`(`.is-in`) | **The per-method panel bodies that render inside `.gv-phasebody` on a project page**, source-grounded on the live FO ideation/proposals/survey/volunteering/common-ground captures and verified via the `fo-project/*` checkpoints. **One `.gv-ideacard`** (162×162 thumb, horizontal media card) is shared by ideation **list/feed** and **proposals**; **one `.gv-react`** (circular 35×35 like/dislike + count + comment count). **Feed** = idea column + 310px `.gv-feedfilter` sidebar; **List/Map** `.gv-viewseg` toggle; **proposals** add the signature `.gv-threshold` bar + `.gv-statuspill`; **volunteering** = `.gv-cause` cards with the green dark-text `.gv-btn.volunteer`; **survey** = the tenant-primary `.gv-surveyband` CTA strip (open single-method survey shows **no** stepper); **common ground** hosts an external Polis iframe (reconstructed page-local, non-grounded). The flagship `pages/project-page/` carries the corrected shell (right-aligned manager controls, full-bleed banner photo, two-col title block with CTA stack, the `.gv-partbar` sticky footer). |
| **Back-office config forms** (Page) | `pages/bo-project-phase/` | `.gv-bo-formhead`/`-subhead`/`-caption`/`-qlabel`/`-help` · `.gv-bo-select` · `.gv-bo-banner`(`--ai`) · `.gv-bo-methods`/`.gv-bo-methodcard`(`.is-selected`) · `.gv-bo-togglerow` · `.gv-bo-tags`/`.gv-bo-tag` · `.gv-bo-imageup`/`.gv-bo-dropzone` · `.gv-bo-table`(`.is-bordered`)/`__check`/`__num`/`__title` `.gv-bo-th--sort`(`.is-sorted`) · `.gv-bo-filterrail`/`__tabs`/`__tab`/`__item`/`__count` · `.gv-bo-toolbar`/`-search`/`-pager` · `.gv-bo-headrow`/`-eyebrow`/`-count-line`/`-link` · `.gv-bo-importlist`/`.gv-bo-importcard` · `.gv-bo-statgrid`/`.gv-bo-stat` · `.gv-bo-chartcard` · `.gv-bo-pane`/`-card`/`-empty`/`-eventrow` · `.gv-bo-pagehead`/`-pagetitle`/`-pagelede` · `.gv-btn.success` `.gv-btn.size-s` | **The deep project-configuration editor**, source-grounded on the live captures (`bo-phase-setup`, `bo-project-{general,audience,events,ideas}`, `bo-input-form-builder`, `bo-phase-insights`) and verified via the `bo-project-phase/*` checkpoints. One clickable page: all six **project top-tabs** swap panels, and the **Timeline → phase** panel now has four wired **sub-tabs** (`.pp-sub`): **Setup** (rich method picker, anonymity, action toggles, likes/dislikes, similar-input detection, views, sorting, attachments), **Input manager** (AI callout, filter bar, bordered filter rail + sortable posts table — checkbox, assignee dropdowns, like/dislike counts), **Input form** (Edit-form entry + Paper-OCR/Spreadsheet import cards), **Insights** (SHARED stat cards + participation chart). Plus **General** (full settings form), **Audience** (participants table + pager), **Messaging** (empty state), **Events** (event list). Pure assembly on `.gv-bo` primitives + the config classes. |
| **Event detail page** (Page) | `pages/event-detail/` | `.gv-eventdetail`/`__main`/`__side` · `.gv-eventdetail__from` (16px/#333 eyebrow) · `.gv-eventdetail__projlink` (14px/cool-grey "Go to the project") · `.gv-eventdetail__title` (30px/700/#333 event H1) · `.gv-eventdetail__datecard` (#EDEFF0 app-canvas panel, 3px) · `.gv-eventdetail__datestylized` (probed container, 14px/.87) + `.gv-eventdetail__date`/`.d`(34px/700)/`.m`(16px/#767676)/`.t`(18px/600) · `.gv-eventdetail__actions` · `.gv-eventdetail__share`/`__sharebtns` · reuses `.gv-prose` · NEW primitive `.gv-btn.primary-text` (transparent tenant-primary text button) | The single front-office **event detail** page (`/events/:id`), reached from a Past-event card, source-grounded on `wietsedemo.govocal.com/en/events/<id>` (capture `fo-event-detail`, Cultuurconnect tenant), verified via `fo-event-detail/{title,date-stylized}`. Two-column layout: main = "From <project>" eyebrow + "Go to the project" link + event H1 + rich-text `.gv-prose` body; 350px sidebar = a **stylized date block** on the grey app-canvas panel (big day number / month / time, with an sr-only date sentence) + an **"Add to calendar"** `.gv-btn.primary-text` + a **"Share this event"** block. Net-new = the `.gv-eventdetail-*` layout atoms (`govocal-ui.css`) + the `.gv-btn.primary-text` variant (`govocal-primitives.css`, also used by the "Go back" link). Pure assembly; header/footer dragged in; re-skins per `?theme=`. |
| **Events page** (Page) | `pages/events/` | `.gv-events-page__title` (80px/700/tenant-primary, `--gv-fs-display`) · `.gv-events-page__section` (25px/700 section h2) · `.gv-events-page__sectionhead` · `.gv-eventfilters` · `.gv-filter-pill` (FO pill-dropdown variant — 24px pill, tenant-primary fill + chevron; `[aria-expanded=true]` flips it) · reuses `.gv-events__grid`/`__empty` + `.gv-event-card`/* | The standalone front-office **/events** page, source-grounded on `wietsedemo.govocal.com/en/events` (capture `fo-events`), verified via `fo-events/{title,section}` checkpoints. Anatomy: huge tenant-primary page **title** → **"Upcoming and ongoing events"** section with a **filter bar** (Date + Projects pill dropdowns) and the live **empty-state** → **"Past events"** auto-fill **grid** of `.gv-event-card` (reused unchanged). Net-new = the display-scale title, the section h2s, and the FO `.gv-filter-pill`. Pure assembly; header/footer dragged in; re-skins per `?theme=`. |
| **Projects & folders list page** (Page) | `pages/projects-list/` | `.gv-projlist__title` (centered 34px/500/#474747 page H1, `--gv-text-pagetitle`) · `.gv-searchfield`/`__icon` (full-width search wrapper: a `.gv-input` + right-aligned magnifier) · **reuses** `.gv-ptoolbar`/`.gv-tabs`/`.gv-tab` (Published/Archived/All status tabs w/ counts) + `.gv-filterbar`/`.gv-filter-btn` (Tag/District selectors) + `.gv-pgrid` of `.gv-pcard.boxed` (timer · `.gv-status-label.finished` · outlined `.gv-btn.primary-outlined` CTAs e.g. "View the issues" · `.gv-bubbles`) + header/footer | The standalone front-office **"All projects"** index (`/projects`), source-grounded on wietsedemo `…/projects` (capture `fo-projects-list`), verified via `fo-projects-list/{search,filter-btn,card}`. Centered "Projects" title → full-width "Search projects and folders" field (magnifier) → the homepage toolbar (status tabs + Tag/District filter selectors) → a static 3-up `.gv-pgrid` of boxed project & folder cards (the live list is virtualized — only ~7 of 22 render). **Net-new = just the `.gv-projlist__title` + `.gv-searchfield` atoms (`govocal-ui.css`) + the `--gv-text-pagetitle` token**; the toolbar and card grid are reused wholesale from the homepage. Pure assembly; re-skins per `?theme=`. NOTE: this tenant has NO folders — the folder card is reconstructed from the homepage folder-card pattern. |
| **Perspectives — Issue-Mapping feed** (Page) | `pages/perspectives-feed/` | `.gv-issuefeed`/`__rail`/`__back`/`__title`/`__participants`/`__question`/`__themelist` · `.gv-themecard`(`.is-active`)/`__swatch`/`__main`/`__head`/`__name`/`__count`/`__desc`/`__bar` (inline `--gv-themecard-swatch`/`--gv-themecard-bar`) · `.gv-issuecanvas`(`.is-detail`)/`__view`/`__pile`/`__add` · `.gv-sticky`(`.is-raised` · `.lavender`/`.mint`/`.pink`/`.grey`, inline `--gv-sticky-bg`)/`__author`/`__authorname`/`__emoji`/`__title`/`__excerpt`/`__react`/`__reactbtn` · reuses `.gv-bubbles`/`.av` + `.gv-prose` + `.gv-btn` | The dedicated full-bleed **Perspectives / Issue-Mapping** screen, source-grounded on wietsedemo `…/ideas-feed` (capture `fo-perspectives-feed`), verified via `fo-perspectives/{sticky-card,like-button}`. Two-column split: a **30% rail** (Back · "Issue Mapping" title · participant `.gv-bubbles` · question + intro `.gv-prose` · ranked `.gv-themecard` category cards w/ emoji swatch + count + mini share bar) beside a **70% dotted-grid `.gv-issuecanvas`** that floats pastel **sticky-note** idea cards (`.gv-sticky` — 320px/20px/radius-3px, four pastel variants, resting vs `.is-raised` shadow, author chip + title + excerpt + row-reverse dislike/like reactions). Two canvas states wired: a raised **detail** card ⇄ the masonry **pile** (Back / click-a-note toggle), plus a floating "Add an idea" CTA. Pure assembly; header/footer dragged in; re-skins per `?theme=`. || **Perspectives — in-timeline phase entry** (Page) | `pages/perspectives-entry/` | `.gv-pviewtoggle`/`__btn`(`.is-active`, `.e2e-list-map-viewbuttons` wrapper, `#view-tab-1/2`) · reuses `.gv-phases`/`.gv-stepper`/`.gv-phasepanel` (ribbon) + `.gv-feed__count` + `.gv-issuecanvas`/`__pile` + `.gv-sticky`/* + `.gv-idealist`/`.gv-ideacard`/* + `.gv-partbar` + header/footer | The Perspective phase **entry point INSIDE the project timeline** (vs the dedicated full-bleed feed). The standard project-page shell + the `.gv-methodband` ribbon (the project's real 5 phases: Issue Mapping[current] · Issue Voting · Issue Reporting · Discussion Workshop · Recommendations) where the active phase BODY is the Perspective view: a "178 ideas" `.gv-feed__count` + the **List \| Perspective** view toggle + the `.gv-issuecanvas__pile` of pastel `.gv-sticky` notes INLINE under the phase; the List tab swaps the same ideas to a flat `.gv-idealist`/`.gv-ideacard`. Source-grounded on wietsedemo `…/perspectives/1` (capture `fo-perspectives-entry`), verified via `fo-perspectives-entry/{view-toggle,view-tab,ideas-container}`. **Only net-new = the `.gv-pviewtoggle`** (white wrapper + own-shadow pill tabs; inactive `var(--gv-tenant-text)`, active fill `#EFD015` Perspective accent literal); everything else is reused canonical. Pure assembly; re-skins per `?theme=`. |
| **Back-office section pages** (Pages) | `pages/bo-{dashboard,projects,input-manager,users,messaging,reporting,community-monitor,inspiration-hub,tools,pages-menu,settings,notifications}/` | `.gv-bo-tabs--top` (section tab strip, 60px/#FBFBFB, 44px inset) · `.gv-bo-table.is-list` (striped white list table, bordered panel, 16/12 headers) · `.gv-bo-listrow`/`__handle`/`__main`/`__title`/`__sub`/`__actions` · `.gv-bo-status`(`.is-published`/`.is-archived`) + `.gv-bo-substack` · `.gv-status-label.draft`/`.published` · reuses the BO chrome (`.gv-bo-side`/`-shell`/`-pagehead`/`-pagetitle`/`-pagelede`/`-field`/`-multiloc`/`-filterrail`/`-table`/`-pager`/`-statgrid`/`-chartcard`/`-formhead`/`-subhead`/`-select`/`-toggle`) · per-page `bo-chrome.js` renders the linkable sidebar | **The 12 top-level admin screens**, source-grounded on live captures (`bo-{dashboard,projects-list,input-manager,users,messaging,reporting,community-monitor,inspiration-hub,tools,pages-menu,settings,notifications}`) and verified via `bo-dashboard/tabstrip`, `bo-projects/table-*`, `bo-messaging/draft-label` checkpoints. All share the BO sidebar chrome (`bo-chrome.js` sets the active item + links sections together). **Dashboard** (KPI + chart cards), **Projects** (striped list table + filters + New project), **Input manager** (cross-project filter rail + posts table), **Users** (role/groups rail + users table), **Messaging** (Custom/Automated tabs + DRAFT rows), **Reporting** (report rows), **Community monitor** (Health Score + sentiment battery), **Inspiration hub** (example-project gallery), **Tools** (integration cards + locked premium), **Pages & menu** (navbar item rows), **Settings** (tabbed General config matched to the live capture), **Notifications** (activity feed). Pure assembly. Dashboard content + Notifications are **reconstructed** (the demo tenant renders no dashboard data; `/admin/notifications` is a bell flyout with no standalone page) — re-capture on a tenant with data to pin exact values. |

| **Voting method body** | `components/approval-voting/` | `.gv-voteoptions`/`.gv-voteoption`(`__head`/`__title`/`__chevron`/`__panel`(`.is-open`)) · `.gv-tally`(`__title`/`__caption`/`__count`/`__sub`) · `.gv-voteresults`/`.gv-voteresult`(`__thumb`/`__rank`/`__body`/`__title`/`__pct`/`__track`/`__fill`/`__foot`) · reuses `.gv-btn.text` + `.gv-react__comment` + `.gv-title.h2` | **The per-method panel that renders inside `.gv-phasebody` for a VOTING phase** (approval/single/budget) — the gap left by ideation/proposals/survey/volunteering/common-ground. Source-grounded on wietsedemo `…/approval-voting…` (capture `fo-approval-voting`, Cultuurconnect tenant, primary #112D7E), verified via `fo-approval-voting/{option-title,tally-card,tally-title,result-card,rank-badge}`. Two parts: (1) a collapsible **options accordion** (`.gv-voteoption` white cards — 21/700/#474747 title + 24px right-chevron rotating 90° on open, expandable `__panel`; page-local toggle script, no JS in canonical CSS); (2) the **results** state — a `.gv-tally` "Final tally" card (white/20px/soft-shadow: 18/700/tenant-primary heading + 16px caption + 34px/700/#333 count `--gv-fs-xxxxl` + sub) above a 2-up `.gv-voteresults` grid of ranked `.gv-voteresult` cards (white/3px/17px/soft-shadow flex row: rank badge `__rank` bg `--gv-blue-500` #044D6C / 3px / white #N · thumbnail · 16/700 title · `__pct` 14/700/tenant-primary line · a NEW percentage `__track` bar [tenant-primary tinted 25% + 1px tenant-primary border, 4px radius, 8px] with solid tenant-primary `__fill` — distinct from `.gv-threshold__bar` · `.gv-btn.text` "Read more" + `.gv-react__comment`). Net-new in `govocal-ui.css`; reuses `.gv-btn`/`.gv-react`/`.gv-title` + `--gv-blue-500` token; no base mutated. Pure assembly; re-skins per `?theme=`. |

| **Project-page embedded events section** | `components/voting/` | `.gv-project-events`/`__sec`/`__rule`/`__empty` · **reuses** `.gv-events-page__section` (25/700/32.5/#474747 h2) + `.gv-events-page__sectionhead` + `.gv-filter-pill` (Date dropdown) + `.gv-events__grid`/`.gv-event-card` | **The "Upcoming and ongoing events" + "Past events" sections a project page renders BELOW the phase body**, on the project's own content column (a chrome variant the dedicated `/events` page doesn't cover). Source-grounded on wietsedemo `…/option-analysis-voting…` (capture `fo-voting`), verified via `fo-voting/{events-section,events-section-title,events-date-filter,events-card-title,events-empty}`. **Pure assembly** — reuses the events atoms wholesale; **net-new is ONLY** the `.gv-project-events` scaffold (flex column, **gap 48px**, max-width 1166px — probed), the `__rule` divider under each head, and the **plain** `.gv-project-events__empty` line (16/#596B7A/lh 26 — distinct from the dashed-box `.gv-events__empty`). Drop the `<div class="gv-project-events">` after `.gv-phasebody`; stack one `.gv-project-events__sec` per heading. Re-skins per `?theme=`. |
| **Poll method body** | `components/poll/` | `.gv-poll`/`__question`/`__qhead`/`__num`/`__label`/`__options`/`__send` · **reuses** `.gv-radio`/`.gv-checkbox` (option choices) + `.gv-btn.primary.full` (Send) + `.gv-partbar` (sticky "Open for participation · Take the poll") | **The per-method panel that renders for a POLL phase** — a stack of question cards each holding single/multi-choice options + a full-width Send. Source-grounded on wietsedemo `…/poll-mobility-plan…` (capture `fo-poll`, Cultuurconnect tenant, primary #112D7E), verified via `fo-poll/{form,question,option,send}`. The `.gv-poll` form is a centred flex column (#474747 `--gv-text-pagetitle`, 14px/19.999 inherit) of `.gv-poll__question` white cards (20px pad, 3px radius, soft `--gv-shadow`, **no fieldset border**); each card = a `.gv-poll__qhead` (25px square **#F2F2F2** order badge `__num` [16/600, one-off grey, NOT the #F4F6F8 token] + 18/600 `__label`) over a `.gv-poll__options` list. **Options reuse `.gv-radio`/`.gv-checkbox` primitives** (16px, indented 35px, line-height/align reset to the live label flow — scoped, base untouched). Send = `.gv-poll__send` row wrapping `.gv-btn.primary.full` (`disabled` until complete). The sticky participation band **reuses the existing `.gv-partbar`** (`.e2e-participation-cta-poll` rendered empty in this capture). Net-new in `govocal-ui.css`; no base primitive mutated. Pure assembly; re-skins per `?theme=`. |
| **Idea card** | `components/idea-card/` | `.gv-ideacard`/`__thumb`/`__body`/`__title`/`__head`/`__avatar`/`__meta`/`__author`/`__sep`/`__time`/`__excerpt`/`__foot` · `.gv-react`/`__btn`/`__count`/`__comment` | **The shared idea/proposal card** — 162×162 thumb, author meta, excerpt, and a footer of circular 35×35 like/dislike `.gv-react` controls + comment count. Surfaces the canonical block that previously lived only inside `pages/project-page/` & `project-proposals/`; verified via `fo-project/ideacard*`. Pure assembly; re-skins per `?theme=`. |
| **Idea feed** | `components/idea-feed/` | `.gv-feed`/`__count`/`__col` · `.gv-feedfilter`/`__group`/`__label`/`__btn`/`__count` · `.gv-viewseg`/`__tab` · `.gv-idealist` | **The ideation feed layout** — a `.gv-feed__count` + List/Map `.gv-viewseg` over a two-column grid pairing the `.gv-idealist` of cards with a 310px `.gv-feedfilter` sort/status/topic sidebar. Surfaced from `pages/project-page/`; verified via `fo-project/{feed-count,feed-sidebar,viewseg}`. Pure assembly. |
| **Proposal threshold** | `components/proposal-threshold/` | `.gv-threshold`/`__icon`/`__count`/`__bar`/`__fill` · `.gv-statuspill`(`.open`/`.expired`) | **The proposals signature votes-needed bar** — vote icon + count-over-target + tenant-tinted fill, paired with an open/expired status pill. Surfaced from `pages/project-proposals/`; verified via `fo-project/proposals-threshold-count`. Shows open, reached, and expired states. |
| **Volunteer cause** | `components/volunteer-cause/` | `.gv-cause`/`__media`/`__img`/`__badge`/`__body`/`__title`/`__count`/`__desc`/`__cta` · `.gv-btn.volunteer`(`.is-in`) | **The volunteering opportunity card** — photo + participant badge, title, count, description, and the green dark-text `.gv-btn.volunteer` that toggles to a withdrawn `.is-in` state. Surfaced from `pages/project-volunteering/`; verified via `fo-project/volunteer-{desc,btn}`. |
| **Survey band** | `components/survey-band/` | `.gv-surveyband`/`__inner`/`__status`/`__dot`/`__cta` · reuses `.gv-btn.on-color` | **The "Take the survey" CTA strip** on a project-page survey phase — a live-dot status on the left, an on-color CTA on the right (distinct from the `survey-fields` field kit). Surfaced from `pages/project-survey/`; verified via `fo-project/survey-band*`. |
| **Participation bar** | `components/participation-bar/` | `.gv-partbar`/`__inner`/`__status` · reuses `.gv-btn.on-color` | **The sticky project-page action footer** — participation status on the left, a primary on-color CTA on the right that pins as residents scroll the phase. Surfaced from `pages/project-page/` (also reused by poll). Pure assembly. |
| **Avatar overflow-count bubble** | `components/avatar-overflow-bubble/` | `.gv-bubbles .count.bubble` (`.sm`/`.xs` scale) · reuses `.gv-bubbles`/`.av`/`.count`/`__label` | **The participant-overflow count rendered AS a solid cool-grey circle capping the avatar overlap stack** (vs the bare `.count` adjacent text). Source-grounded on Falkirk (`participateplus.falkirk.gov.uk`, capture `fo-falkirk-bubbles-overflow`, probed `div.uItlk`), verified via `fo-falkirk-bubbles-overflow/count-circle`. The circle = `--gv-cool-grey-600` bg, **38px**, **2px** white border, radius **50%** (the base `.count` already paints this); the `.bubble` modifier normalises the abbreviated white text to the measured **11px/400** (base is 11px/700). Holds abbreviated (`19.8k`) or full (`7443`) counts. **Cross-tenant SYSTEM** — the same stack-cap treatment on Falkirk/Linz/St&nbsp;Louis/Copenhagen (one product, re-skinned); SKIN re-themes per `?theme=`. Net-new = just the `.count.bubble` modifier (`govocal-primitives.css`) — base `.gv-bubbles`/`.av`/`.count` untouched. Pure assembly. |
| **Sticky note** | `components/sticky-note/` | `.gv-sticky`(`.is-raised` · `.lavender`/`.mint`/`.pink`/`.grey`, inline `--gv-sticky-bg`)/`__author`/`__authorname`/`__emoji`/`__title`/`__excerpt`/`__react`/`__reactbtn` | **The pastel Perspectives idea note** — 320px/radius-3px card, author chip + emoji + title + excerpt + row-reverse react counts, in resting vs `.is-raised` and four pastel variants. Surfaced from `pages/perspectives-feed/`; verified via `fo-perspectives/{sticky-card,like-button}`. |
| **Theme card** | `components/theme-card/` | `.gv-themecard`(`.is-active`)/`__swatch`/`__main`/`__head`/`__name`/`__count`/`__desc`/`__bar` (inline `--gv-themecard-swatch`/`--gv-themecard-bar`) | **The ranked Perspectives category card** — emoji swatch + name + response count + a mini share bar; the first shows the active selection. Surfaced from `pages/perspectives-feed/`. Pure assembly; re-skins per `?theme=`. |
| **Issue canvas** | `components/issue-canvas/` | `.gv-issuecanvas`(`.is-detail`)/`__view`/`__pile`/`__add` · `.gv-issuefeed`/`__rail`/* · hosts `.gv-sticky` notes | **The dotted-grid Perspectives canvas** that floats sticky-note ideas in a masonry `__pile`, with a floating "Add an idea" button and an `.is-detail` raised state. Surfaced from `pages/perspectives-feed/`. Pure assembly. |
| **Event card** | `components/event-card/` | `.gv-event-card`/`__media`/`__date`(`.m`/`.d`/`.y`)/`__rsvp`/`__body`/`__title`/`__row` · `.gv-events__grid`/`__empty` | **The two-tone-date-chip event card** — date chip pinned over the media, clock/location/link/registrant rows + Register CTA, in the events grid with its empty state. Reused on homepage, /events & project pages. Surfaced from `pages/homepage/` & `events/`; verified via `homepage-event-title` + `fo-events/*`. |
| **Event card — bordered EventsWidget** | `components/event-card-bordered/` | `.gv-event-card.bordered`(`.is-imageless`) · `.gv-event-datechip--stacked`/`__top`/`__day`/`__month`/`__year` · `.gv-event-info-panel` · reuses `.gv-event-card__titlerow`/`__title`/`__row` + `.gv-btn.primary.full`(`[aria-disabled]`) | **The shared EventsWidget treatment used by Copenhagen / Linz / Falkirk** (one product, re-themed), source-grounded on Copenhagen `kobenhavntaler.kk.dk` (capture `fo-cph-events-card`), verified via `fo-cph-events-card/bordered-frame`. STRUCTURAL cross-tenant frame distinct from the borderless wietsedemo card: each event is a **bordered** white `<li>` (digest #15: 1px&nbsp;#CCC `--gv-event-card-border`, 6px radius) holding a **3-tier stacked date chip** (`.gv-event-datechip--stacked` — grey `#F5F6F7` `__top` tier with `__day` 14/400 + uppercase `__month` 14/500, both tenant-primary text, radius `3 3 0 0`; over a tenant-primary `__year` band, white, radius `0 0 3 3` — digest #17/#19/#20) and a **#F4F6F8 “Date & time” info panel** (`.gv-event-info-panel`, pad `12 16 4`, digest #22). Title (18/700 tenant-primary) sits left of the chip in `__titlerow`. **Imageless degrade** (`.is-imageless`, Falkirk) drops `__media`; closed events use the canonical `.gv-btn[aria-disabled]` Register. **Net-new = the `.bordered` variant + the `--stacked` chip + `.gv-event-info-panel` + `--gv-event-card-border` token** (`govocal-ui.css`/`govocal-tokens.css`) — the base card and the `.m`/`.d`/`.y` overlay chip are untouched. The skin (palette/font/language) re-themes per `?theme=` (CPH #000C2E / Linz #604596 / Falkirk #198754). Pure assembly. |
| **Spotlight** | `components/spotlight/` | `.gv-spotlight`/`__inner`/`__eyebrow`/`__title`/`__lead`/`__actions`/`__media`(`--placeholder`)/`__chip` · adjacent `.gv-bubbles` + `.gv-progress` | **The homepage "currently working on" spotlight** — copy column (eyebrow/title/lead/actions + avatar-bubble count) beside a media tile with a no-image placeholder. Surfaced from `pages/homepage/`. Pure assembly; re-skins per `?theme=`. |
| **Accordion** | `components/accordion/` | `.gv-accordion`/`.gv-acc__item`(`[open]`)/`__head`/`__q`/`__chev`/`__body` | **CSS-only `<details>` FAQ accordion** — collapsed by default, chevron rotates on open, one source-of-truth body type. Inside `.gv-projdesc` the head/body step up to 18px to match surrounding copy. Surfaced from `pages/project-page/`. |
| **Content-Builder render layer** | `components/content-builder-render/` | `.gv-cb-frame` · `.gv-cb-row`(`.cols-1`/`.cols-2`/`.cols-3`) · `.gv-cb-col` · `.gv-cb-textbox`(`.ql-align-center`/`-right`/`-justify`) · `.gv-cb-image` · `.gv-cb-whitespace`(`.is-small`/`.is-medium`) | **What the back-office Content Builder OUTPUTS onto a live FO homepage/custom page** (not the editor chrome — that's `.gv-bo-cb-*`). A full-width `.gv-cb-frame` holds a vertical stack of `.gv-cb-row`s of generic cells: 1/2/3-column rows (`.cols-N`) of `.gv-cb-col`s containing text-box / image / white-space cells. Cross-tenant STRUCTURE, source-grounded on Copenhagen (`fo-cph-cb-home`): single-column 588, multi-column rows constrained to the **1200** content measure with a **24px** gap (588+24+588), text-box padding `24px 0`, white-space `12px`/`24px 0`. The skin (font, colours) re-themes per `?theme=`. **Every other FO widget mounts INTO these cells** — the scaffold for assembling CB homepages + custom pages. Net-new in `govocal-ui.css`; pure assembly. Verified via `fo-cph-cb-home/*`. Reference page: `pages/fo-cph-cb-home/`. |
| **Banner** | `components/banner/` | `.gv-banner`/`__art`(`svg`)/`__sticker` | **Image-only project hero** (distinct from `.gv-hero` which carries title + CTA) — a full-width teal-gradient art panel (1150×300, `--gv-shell-w` capped) holding an illustration `<svg>` + an optional rotated campaign sticker. Pure assembly. |

## How to reuse in a prototype

A **prototype** (unlike a library demo) IS self-contained — copy the assets in:
1. Copy `govocal-tokens.css`, `govocal-primitives.css`, `govocal-ui.css`,
   `govocal-themes.js` (+ `govocal-logo.svg` if you use the footer, + `govocal-cookies.js`
   on resident-facing prototypes). `govocal-ui.css` `@import`s primitives, so copy both.
   **Back-office UI:** also copy `govocal-bo.css` and scope markup under `.gv-bo` (fixed
   teal/navy theme, not `?theme=`).
2. Grab the markup from `skills/govocal-ui/components.md` (or a demo `index.html`) and
   theme via `--gv-tenant-primary|secondary|text` — never hardcode brand hex.
3. Cities can also override `--gv-font-family` (e.g. Vienna ships `WienerMelange_W_Rg`).

## Adding a component

1. `components/<name>/index.html` — demo that **references canonical** via
   `../../skills/govocal-ui/<asset>` (don't copy assets in).
2. Add its styles to the right canonical file: shared atoms → `govocal-primitives.css`,
   FO → `govocal-ui.css`, BO chrome → `govocal-bo.css`. The demo must only **use** `.gv-*`,
   never define them.
3. Add a snippet to `skills/govocal-ui/components.md` and a row to this table.
4. `npm run lint` (must pass) → `npm run build` — `build.js` auto-discovers the folder.
