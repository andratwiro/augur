{
  "name": "augur",
  "version": "1.0.0",
  "private": true,
  "description": "Augur — build/deploy platform composing the Go Vocal design system + workspace into a private static site.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "shoot": "node scripts/shoot.mjs",
    "shoot:stale": "node scripts/shoot.mjs --stale",
    "og": "node scripts/og.mjs",
    "og:stale": "node scripts/og.mjs --stale",
    "review": "node scripts/review.mjs",
    "dev": "node build.js && npx --yes serve dist",
    "deploy": "set -a && . ./.env.deploy && set +a && node scripts/shoot.mjs --stale && node scripts/og.mjs --stale && node build.js && npx wrangler pages deploy dist --project-name govocal-prototypes --branch main"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
