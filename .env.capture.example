# Capture-harness config (System-building mode) — TEMPLATE.
# Copy to `.env.capture` (gitignored) and fill in the demo-platform creds, then
# `npm run capture` / `npm run verify` work. NEVER commit the real .env.capture.
#
# Demo platform we capture GoVocal's UI from (City of Raleigh test tenant).
GOVOCAL_BASE=https://uxusertesting.govocal.com
GOVOCAL_SIGNIN=https://uxusertesting.govocal.com/en/sign-in
GOVOCAL_USER=
GOVOCAL_PASS=
